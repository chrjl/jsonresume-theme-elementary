{{#if resume.languages.length}}
<section id='languages'>
  {{!-- <h2>Languages</h2> --}}

  <div class='horizontal-description'>
    <div class='name'>
      languages
    </div>

    <div class='keywords'>
      {{#each resume.languages}}
      {{language}}<!--
     -->{{#if fluency}}<span class='fluency'> ({{~fluency~}})</span>{{/if}}<!--
     -->{{#if @last}}{{else}},{{/if}}
      {{/each}}
    </div>
  </div>
</section>
{{/if}}